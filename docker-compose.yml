services:
  mangafill:
    build: .
    container_name: mangafill
    ports:
      - "8788:8788"
    env_file:
      - .env
    volumes:
      - ./data:/data
      - ./uploads:/input
      - ./config:/app/config
      - ./fonts:/app/fonts
    restart: unless-stopped

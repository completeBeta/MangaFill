# GPU worker dependencies. torch/torchvision come from the base image
# (pytorch/pytorch:2.5.1-cuda12.4-cudnn9-runtime) so they are NOT listed here —
# pinning torch here would overwrite the CUDA build with a CPU wheel.
fastapi>=0.110
uvicorn[standard]>=0.29
python-multipart>=0.0.9
pillow>=10.0
numpy<2.0
transformers>=4.49
manga-ocr>=0.1.16
opencv-python-headless>=4.8
# Korean + Chinese OCR (PP-OCRv5/v6) — runs on CPU via the ONNX runtime engine
# (paddle native CPU inference is broken: PIR/oneDNN crash). paddleocr pulls in
# paddlepaddle (CPU) as a dependency; inference itself goes through onnxruntime.
paddleocr>=3.0
onnxruntime>=1.16
